# State files (runtime, not tracked)
state/feedback/
state/scores/
state/reports/

# Keep directory structure
!state/.gitkeep
!state/feedback/.gitkeep
!state/scores/.gitkeep
!state/reports/.gitkeep

# Logs (contain agent output, potentially sensitive)
ralph-*.log
*.log

# Environment
.env
.env.*
